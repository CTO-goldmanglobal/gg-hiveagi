# P2P Exchange dependencies
# Core exchange: stdlib only (urllib, json, hashlib, base64, pathlib)
# Real IPFS: install kubo daemon separately (https://docs.ipfs.tech/install/)
#
# Trust layer (signed manifests — identity.py, appreciation.py):
cryptography>=42.0

# Testing:
pytest>=8.0
